class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        map<int,bool> kiran;
        int n=nums.size();
        for(int i=0;i<n;i++){
            if(kiran[target-nums[i]]==true || kiran)
        } 
    }
};